# Task index

One line per task, newest first. Scan this file to find prior work; each task
directory is a self-contained journal (runbooks, session logs, analysis
scripts). Conventions for adding/updating entries live in [CLAUDE.md](CLAUDE.md).

| task | status | dates | outcome |
|---|---|---|---|
| [2026-07-fallen-arch-exercises](2026-07-fallen-arch-exercises/fallen-arch-exercises.md) | **active** | 2026-07-14 → | Personal: leg-strength program for fallen arches (overpronation → knee pain via the kinetic chain). Daily ~5-min foot mini-routine (short foot, toe yoga, single-leg balance) + 3×/week ~20-min single-leg strength session; progression plan and exercises-to-skip in the doc. |
| [2026-07-stacks-in-clickhouse](2026-07-stacks-in-clickhouse/stacks-in-clickhouse.md) | **iteration 1 passed** | 2026-07-14 → | Compute stacks in ClickHouse + Python glue instead of Flink (architecture C vs [working-set-state](2026-07-stacks-working-set-state/stacks-working-set-state.md)'s A/B). Scope moved XRP→**ETH**. Spike done: exact-int fold replica of `HandlerOneAccountChange`+`groupAndCompress`, golden tests 14/14, and a 10-clean-address focus group folded genesis→2016-03 matched stage `eth_stacks` **byte-exact (1338/1338 rows** incl. nonces/odt/Float64 amounts/txID). State table = output re-keyed (`ver = block*2+deleted`) worked as designed. Committed `d1ce81a9` @ `clickhouseStacks` (etherbi-flink `clickhouse-stacks/`, architecture doc inside; stage-tables ledger in TABLES.md). Bonus finding: stage's dedup coin-flip (rewards lost at (0,0)) is a **fossil of pre-#227 san-chain-exporter data** — prod re-exported clean (verified), stage never wiped. Next: full-chain shadow vs **prod** (deterministic input, digest compare); task-doc bootstrap section is current. |
| [2026-07-stacks-working-set-state](2026-07-stacks-working-set-state/stacks-working-set-state.md) | **plan approved** | 2026-07-14 → | Future-proof the stacks job for high-volume chains: break the O(chain-age) state growth ([odt bucketing](2026-06-odt-bucketing-xrp/verifying-the-batched-odt-migration.md) alone gave limited gains — −27% live segments — because dormant addresses dominate). Direction: ForSt (S3-primary state, Flink 2.3) + async State V2 with true `(contract, address)` keying ≡ the "per-address out-of-block-order" restructure; CH state-rebuild (enabled by the stack-sorted-by-ots insight) demoted to bootstrap/DR. **2026-07-16: full migration plan approved** ([forst-async-migration-plan.md](2026-07-stacks-working-set-state/forst-async-migration-plan.md)) — ForSt as the **sole** backend + **every** keyed operator → async State V2, dropping the V1 keyed-state API. Correction: DataStream windows *can* use async state on 2.3 (FLIP-488), so windowing needn't be removed (Path W vs Path P gated by a Phase-0 spike). odt cohort-batching **reverted** from `batchStacksOdt` (etherbi-flink `25df81fe`; recoverable `788c0ded`, returns in Phase B3). Next: Phase-0 spikes (stateful-window+async, State-Processor-API vs ForSt, replay-from-Kafka), then Phase-A global backend flip. |
| [2026-07-incredible-machine-contraptions](2026-07-incredible-machine-contraptions/incredible-machine-contraptions-windows-bootstrap.md) | handoff written | 2026-07-13 → | Get *Return of the Incredible Machine: Contraptions* (2000, DirectDraw 640×480) running fullscreen. Works under Linux/Wine but only in a screen corner (Wayland can't modeset; gamescope crashes the game), so the doc is a bootstrap handoff for the Windows 11 dual-boot side: use `Contraptions_pc.iso`, mount natively, GPU display-scaling gotcha covered. Linux fallback (dgVoodoo2) sketched, not attempted. |
| [2026-07-semantic-web-watcher](2026-07-semantic-web-watcher/semantic-web-watcher.md) | design settled | 2026-07-13 → | Personal Google-Alerts replacement: daily cron discovers new pages via search engines, host fetches+extracts text, `claude -p` (subscription-billed; one batched triage call + one agentic deep-judge session per run) classifies against a natural-language intent; SQLite seen-store with `judged_at`/`presented_at` split; terminal digest. Design complete (14 decisions — session #2 added the buffered-SQLite-pipeline model, blob store + GC, failure taxonomy, leaf/hub identity); repo created at `~/src/semantic-web-watcher`, code next (M0). |
| [2026-07-genesis-acquisition-price-valuation](2026-07-genesis-acquisition-price-valuation/genesis-acquisition-price-valuation.md) | not started | 2026-07-08 → | Prod defect spun out of the regression guard. PR **#2132** (LEFT ASOF acquisition-price fix, merged 2026-03-02) values cohorts acquired **before any price exists** (ETH genesis/premine, acqTime 2015-07-30) at `acquisition_price = 0` — unmatched ASOF + default `join_use_nulls=0` fills `0` not NULL — so profit-weighted metrics book 100% of their value as profit. ETH `network_profit_loss` 2016: **559M** (HEAD) vs **281M** (served); the +278M is entirely genesis. Fixed the data-loss but introduced a valuation bug. Fix options: `join_use_nulls=1` / explicit exclude / impute genesis price (product call). Confirm blast radius across premine/pre-price chains (BTC, XRP). |
| [2026-07-metrics-regression-guard](2026-07-metrics-regression-guard/metrics-regression-guard.md) | **active** | 2026-07-07 → | Nightly guard: recompute a fixture with HEAD code, diff vs a committed baseline → catch commits that silently change historical metric computation (the next night, not years later). Scoped to transfers/balances/stacks/prices; scope-by-exclusion; write-surface gate. Harness + nightly DAG built (PRs clickhouse-tables#2274, docker-airflow#1714, branch `metricsQA`). ETH recompute of **537** on-chain metrics into `*_guard`. **2026-07-08 pre-merge audit found baseline NOT blessable yet:** (1) `transaction_volume_profit`/`_loss`/`_ratio` were spurious **0** from an intraday-`price_usd` seed gap — **FIXED**: `guard_seed_prices.py` now seeds intraday price too; operator to re-seed + re-run + re-record. (2) NPL 559M-vs-served-281M is a genuine defect → spun out to [genesis-acquisition-price-valuation](2026-07-genesis-acquisition-price-valuation/genesis-acquisition-price-valuation.md); hold the price-weighted family until fixed. Framework topo-sorts *computed* jobs but not *external inputs* (the seed-gap root). **Do not merge until re-recorded.** |
| [2026-06-odt-bucketing-xrp](2026-06-odt-bucketing-xrp/verifying-the-batched-odt-migration.md) | **direction changed** | 2026-06-29 → | Configurable `odt` bucketing in the Flink stacks job, XRP first. Hourly validated end-to-end (L1 + intraday + daily L2, fossils fully explained, HEAD accepted as baseline) but found to destroy the tx-volume-P/L classifiers (−80%; §17–18) → **landing changed to 5-min odt, dt untouched** (config flip; P/L bit-exact, worst deviation ~3e-6; live Flink segments −27% vs unbatched, stacks −31%, seam −35%). ADR amended in etherbi-flink (`6e7aa1a2`). Open: genesis-valuation product call, prod-defect escalation (incl. windowed dollar-age garbage + 2013-14 zero-marketcap days), RocksDB gauge readout, re-validate once 5-min lands. |
| [2026-06-flink-job-build-harness](2026-06-flink-job-build-harness/flink-testability-job-build-harness.md) | not started (plan) | 2026-06-29 → | Step #3 of the test-hardening sequence from the XRP `InvalidTypesException` incident: a `buildsCleanly` harness calling `job.build(config)` offline, a parametrized all-jobs test (15 jobs), and a CI gate so a Flink graph that won't assemble can't merge. Step #2 (lambda → `MapFunction` in `XRPStacks`) already done. Main open question: enumerate jobs by list, classpath scan, or real deploy configs. |
| [2026-06-ltc-stacks-deprecation](2026-06-ltc-stacks-deprecation/ltc-stacks-deprecation.md) | parked (Phase 1 done) | 2026-06 | Replace LTC stacks with balances-derived metrics. Phase 1 (cumsum-safe set) executed + validated; windowed metrics (Phase 2 dip-scan) sketched, awaiting product decisions on the windowed term structure. Origin of `compare_ltc_experimental.py` (template for re-run diffs). |
| [2026-06-onchain-metrics-source-map](2026-06-onchain-metrics-source-map/onchain-metrics-source-map.md) | done | 2026-06-03 | Dependency map coloring every on-chain metric by its raw source (`*_transfers` / `*_balances` / `*_stacks`) — the blast radius of dropping `_stacks`. Key result: all blue metrics root at the single `age_distribution_5min_delta` pivot, and `total_supply` is stacks-dependent only by wiring. Basis for the LTC stacks deprecation. Regen: `extract_source_map.py` (from specs) + `build_metrics_map.py` (curated Graphviz). |
